box:
  id: golang
  ports:
    - "5000"
build:
  steps:
    - script:
        name: go get
        code: |
          go get -t ./...
    - wercker/golint
    - script:
        name: go build
        code: |
          go build ./...
    - script:
        name: go test
        code: |
          go test ./...
